<div class="sidebar-inner">
    <MudNavMenu Class="mud-width-full text-dark">
        <div class="d-flex gap-1 pa-4 sidebar-header">
            <MudImage src="favicon.svg" alt="CMS Logo" Width="40" Height="40" Class="img-fluid" />
            <MudText Typo="Typo.h6" HtmlTag="h2" Class="connect">Connect</MudText>
            <MudText Typo="Typo.h6" HtmlTag="h2" Class="fw-bold cms">CMS</MudText>
        </div>
        <div class="px-4 py-6 sidebar-body">
            
            @* TODO: Add Search field for menus. *@
            @* <div class="input-group flex-nowrap mb-4"> *@
            @*     <span class="search-field"> *@
            @*         <MudTextField @bind-Value="Search" Placeholder="Search" Variant="Variant.Text" Adornment="Adornment.Start" AdornmentIcon="@Icons.Material.Filled.Search" AdornmentColor="Color.Primary" /> *@
            @*     </span> *@
            @*     <span class="input-group-text pa-0"> *@
            @*         <MudImage Src="images/icons/command.svg" Alt="Shortcut key for IOS" Width="39" Height="20" /> *@
            @*     </span> *@
            @* </div> *@

            @if (MenuItems != null)
            {
                foreach (var item in MenuItems)
                {
                    if (item.SubMenus != null && item.SubMenus.Any())
                    {
                        <NavMenuGroup Group="item"/>
                    }
                    else
                    {
                        <NavMenuItem Item="item"/>
                    }
                }
            }
        </div>
    </MudNavMenu>
</div>